---
id: UpdateStudentProfile
name: Update Student Profile
version: 1.0.0
summary: Update GPA, program status, or graduation date for an enrolled student
owners:
  - product

operation:
  method: PATCH
  path: "/students/{studentId}"
  statusCodes:
    - "200"
    - "404"
---
Issued by: **institution partner (via partner API)**

## Envelope

| Field         | Type   | Notes                                                    |
|---------------|--------|----------------------------------------------------------|
| traceId       | uuid   | Propagated from originating request                      |
| correlationId | uuid   | Groups related events across services and workflows      |
| causationId   | uuid?  | ID of the event or command that directly caused this one |

## Payload

| Field          | Type    | Notes                                               |
|----------------|---------|-----------------------------------------------------|
| studentId      | string  | Institution-assigned student ID                     |
| gpa            | number? | Current GPA (0.0–4.0 scale)                         |
| programStatus  | enum?   | active \| graduated \| withdrawn \| on_leave        |
| graduationDate | date?   | ISO 8601 date (set when status → graduated)         |

```json title="schema.json"
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UpdateStudentProfile",
  "type": "object",
  "required": ["studentId"],
  "properties": {
    "studentId":      { "type": "string" },
    "gpa":            { "type": "number", "minimum": 0, "maximum": 4 },
    "programStatus":  { "type": "string", "enum": ["active", "graduated", "withdrawn", "on_leave"] },
    "graduationDate": { "type": "string", "format": "date" }
  }
}
```

<NodeGraph />
